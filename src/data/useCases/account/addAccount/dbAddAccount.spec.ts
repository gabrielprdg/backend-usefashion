describe('DbAddAccount UseCase', () => {
  test('Should call Hasher with correct password', () => {

  })
  test('Should throws if Hasher throws', () => {

  })
  test('Should call AddAccountRepository with correct values', () => {

  })
  test('Should throws if AddAccountRepository throws', () => {

  })
  test('Should return an account if on success', () => {

  })
  test('Should return null if LoadAccountByEmailRepository not return null', () => {

  })
  test('Should call LoadAccountByEmailRepository with correct email', () => {

  })
})
